#Im Model __str__ überschreiben
#zb:

    def __str__(self):
        return "%s : %s" % (self.title, self.owner)

#in der admin.py 
admin.site.register(<appname>)

#bei ....:800/admin anmelden:
    admin_2 : adminadmin2

#zur klasse navigieren
#testen ob man fehlerfreie Objekte der klasse erstellen kann(gui)
#wenn das klappt wird es später einfach mit der verwaltung ;)

#schönes we und bis Di